# Provider presets

Presets are convenience metadata, not proof that every provider/model supports every Codex capability.

The application must probe and verify capabilities at runtime. Provider endpoints can change over time, therefore preset updates belong in the registry layer instead of requiring core releases.
